import { xhr } from "./request";

const home = {
  home_list(data) {
    return xhr({
      url: "/index.....",
      data
    });
  },
  home_add(data) {
    // 添加
    return xhr({
      url: "/index.....",
      data
    });
  },
  home_edit(data) {
    // 添加
    return xhr({
      url: "/index.....",
      data
    });
  },
  home_delete(data) {
    // 添加
    return xhr({
      url: "/index.....",
      data
    });
  }
};

// 实例化再导出
export default home;
